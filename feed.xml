<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://khanhvx09.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://khanhvx09.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-23T00:53:53+08:00</updated><id>https://khanhvx09.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Introduction to Hadoop Distributed File System</title><link href="https://khanhvx09.github.io/blog/2026/hadoop-overview/" rel="alternate" type="text/html" title="Introduction to Hadoop Distributed File System"/><published>2026-09-22T08:00:00+08:00</published><updated>2026-09-22T08:00:00+08:00</updated><id>https://khanhvx09.github.io/blog/2026/hadoop-overview</id><content type="html" xml:base="https://khanhvx09.github.io/blog/2026/hadoop-overview/"><![CDATA[<p>HDFS là hệ thống file phân tán được thiết kế để chạy trên phần cứng thông thường. Ý tưởng chính phía sau HDFS đó là chia nhỏ file thành các khối (blocks) - thường là 128MB và lưu trữ các khối này trên nhiều máy khác nhau. Điều này giúp cho HDFS cung cấp các tính năng quan trọng như: phân tán (distribution), nhân bản (replication), và khả năng chịu lỗi (fault tolerance). Dưới đây là một số concept quan trọng trong HDFS:</p> <h2 id="blocks">Blocks</h2> <p>Trong HDFS, block là đơn vị lưu trữ dữ liệu vật lý nhỏ nhất. Thay vì lưu trữ một tệp tin lớn nguyên khối trên một máy chủ duy nhất, HDFS sẽ tự động chia nhỏ tệp đó thành các khối (blocks) độc lập và phân tán chúng trên nhiều máy chủ (DataNode) khác nhau trong cụm.</p> <p>Lưu ý rằng, nếu một file có kích thước nhỏ hơn kích thước block (ví dụ file dung lượng 30 MB lưu vào block 128 MB), HDFS chỉ chiếm đúng 30 MB dung lượng ổ đĩa, chứ không chiếm trọn 128 MB.</p> <p>Khi có một lớp trừu lượng (abstraction block) trên hệ thống tệp phân tán mang lại nhiều lợi ích:</p> <ul> <li>Một file có thể lớn hơn dung lượng ổ đĩa của một máy chủ duy nhất: bởi vì các khối được phân tán trên nhiều máy chủ, HDFS có thể lưu trữ các tệp tin lớn mà không bị giới hạn bởi dung lượng của một máy chủ duy nhất.</li> <li>Quản lý lưu trữ dễ dàng hơn: Vì kích thước block cố định nên rất dễ tính toán số lượng block có thể chứa trên một ổ đĩa.</li> </ul> <h2 id="name-node">Name Node</h2> <p>NameNode là máy chủ chính trong HDFS, chịu trách nhiệm quản lý thông tin metadata của hệ thống file: filename, file permission và vị trí các block (block location) trên các DataNode. NameNode không lưu trữ dữ liệu thực tế mà chỉ lưu trữ thông tin về cách dữ liệu được phân tán trên các DataNode.</p> <p>NameNode lưu trữ thông tin metadata trong bộ nhớ (RAM) để truy cập nhanh chóng. Khi một client muốn đọc hoặc ghi dữ liệu, nó sẽ liên hệ với NameNode để lấy thông tin về vị trí các block cần thiết. Bởi vì, NameNode là một điểm quan trọng trong HDFS, nếu NameNode gặp sự cố, toàn bộ hệ thống HDFS có thể bị gián đoạn. Do đó, các triển khai HDFS thường sử dụng cơ chế sao lưu (backup) hoặc triển khai NameNode dự phòng (standby NameNode) để đảm bảo tính sẵn sàng cao.</p> <h2 id="data-node">Data Node</h2> <p>DataNode là các máy chủ trong HDFS chịu trách nhiệm lưu trữ dữ liệu thực tế. Mỗi DataNode quản lý các block dữ liệu trên ổ đĩa của nó và thực hiện các thao tác đọc/ghi dữ liệu theo yêu cầu từ client hoặc NameNode. Khi DataNode lỗi, NameNode sẽ phát hiện trình trạng giảm số lượng bản sao (replication) và thực hiện các hành động cần thiết để đảm bảo tính toàn vẹn của dữ liệu. Thông thường, số lượng bản sao của mỗi block mặc định là 3.</p> <p>Dưới đây là một sơ đồ minh họa cách HDFS hoạt động:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/hadoop/hdfs-architecture-480.webp 480w,/assets/img/posts/hadoop/hdfs-architecture-800.webp 800w,/assets/img/posts/hadoop/hdfs-architecture-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/hadoop/hdfs-architecture.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="HDFS Architecture" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <h2 id="high-availability">High Availability</h2> <p>Như đã đề cập ở trên, NameNode là một điểm quan trọng trong HDFS. Nếu NameNode gặp sự cố, toàn bộ hệ thống HDFS có thể bị gián đoạn. Từ Hadoop 2.x trở đi, HDFS hỗ trợ cơ chế High Availability (HA) bằng cách triển khai hai NameNode: một NameNode hoạt động (Active) và một NameNode dự phòng (Standby). Khi NameNode hoạt động gặp sự cố, NameNode dự phòng sẽ tự động tiếp quản vai trò của NameNode hoạt động, đảm bảo hệ thống HDFS vẫn tiếp tục hoạt động mà không bị gián đoạn.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/hadoop/hdfs-ha-480.webp 480w,/assets/img/posts/hadoop/hdfs-ha-800.webp 800w,/assets/img/posts/hadoop/hdfs-ha-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/hadoop/hdfs-ha.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="High Availability" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p>Trong sơ đồ trên, NameNode hoạt động (Active) xử lý tất cả các yêu cầu từ client, trong khi NameNode dự phòng (Standby) luôn đồng bộ với NameNode hoạt động thông qua cơ chế JournalNode. Khi NameNode hoạt động gặp sự cố, NameNode dự phòng sẽ tự động tiếp quản vai trò của NameNode hoạt động, đảm bảo hệ thống HDFS vẫn tiếp tục hoạt động mà không bị gián đoạn.</p> <h2 id="small-files-problem">Small Files Problem</h2> <p>Trong hệ thống thực tế, một vấn đề rất hay gặp phải với HDFS đó là “small files problem” - tình trạng có rất nhiều file có kích thước nhỏ, nhỏ hơn nhiều lần («) so với kích thước block mặc định (128 MB). Ví dụ: hàng triệu file log chỉ vài KB.</p> <p>Vấn đề nằm ở chỗ, dù file nhỏ chỉ chiếm đúng dung lượng ổ đĩa thực tế của nó (như đã nói ở phần Blocks), NameNode vẫn phải cấp phát và lưu trong RAM một lượng metadata <strong>cố định</strong> cho mỗi file và mỗi block, bất kể file đó lớn hay nhỏ: bao gồm một object cho inode (tên file, quyền, thư mục cha…) và một object cho mỗi block thuộc về file đó. Do đó, càng nhiều file nhỏ thì tổng số object metadata càng lớn, trong khi tổng dung lượng dữ liệu thực tế có thể rất khiêm tốn - gây lãng phí RAM của NameNode, làm chậm thời gian khởi động (NameNode phải load toàn bộ metadata vào RAM khi start), và tạo áp lực lớn lên NameNode khi có quá nhiều request nhỏ lẻ.</p> <p>Một con số ước lượng thường được dùng (theo tài liệu của Cloudera/Hortonworks) là mỗi object metadata (file, thư mục, hoặc block) chiếm khoảng <strong>150 bytes</strong> trong RAM của NameNode. Với một file nhỏ (chỉ có 1 block), ta cần 2 object: 1 inode + 1 block, tức khoảng 300 bytes/file.</p> <p><strong>Ví dụ:</strong> với NameNode có 1 GB RAM dành cho việc lưu metadata:</p> \[\frac{1 \text{ GB}}{300 \text{ bytes/file}} = \frac{1{,}073{,}741{,}824 \text{ bytes}}{300} \approx 3.58 \text{ triệu file (block)}\] <p>Để thấy rõ hơn tác động của việc chia nhỏ file, so sánh cùng 1 GB dữ liệu được lưu theo hai cách:</p> <table> <thead> <tr> <th>Cách lưu</th> <th>Số file/block</th> <th>Số object metadata</th> <th>RAM cần cho metadata</th> </tr> </thead> <tbody> <tr> <td>Gộp thành các file 128 MB (đúng kích thước block)</td> <td>8 file → 8 block</td> <td>16</td> <td>~2.4 KB</td> </tr> <tr> <td>Chia thành các file nhỏ 1 MB</td> <td>1024 file → 1024 block</td> <td>2048</td> <td>~300 KB</td> </tr> </tbody> </table> <p>Cùng một lượng dữ liệu, nhưng cách chia nhỏ file tốn RAM NameNode gấp ~128 lần. Đây là lý do các hệ thống thực tế thường có bước gộp file nhỏ (compaction) trước khi ghi vào HDFS, hoặc dùng các định dạng lưu trữ theo container (như SequenceFile, Avro, Parquet) để nhiều bản ghi nhỏ được đóng gói chung vào một file lớn thay vì tạo ra hàng loạt file riêng lẻ.</p>]]></content><author><name>Khanh Xuan Vu</name></author><category term="data-engineering"/><category term="data-engineering"/><category term="data-lake"/><category term="hadoop"/><category term="hdfs"/><summary type="html"><![CDATA[Tổng quan về HDFS]]></summary></entry><entry><title type="html">Overview of Big Data Analytics</title><link href="https://khanhvx09.github.io/blog/2026/spark-overview/" rel="alternate" type="text/html" title="Overview of Big Data Analytics"/><published>2026-09-22T08:00:00+08:00</published><updated>2026-09-22T08:00:00+08:00</updated><id>https://khanhvx09.github.io/blog/2026/spark-overview</id><content type="html" xml:base="https://khanhvx09.github.io/blog/2026/spark-overview/"><![CDATA[<p>Bài viết này sẽ cung cấp một cái nhìn tổng quan về phân tích dữ liệu lớn (big data analytics), bao gồm các khái niệm cơ bản, các công nghệ phổ biến và cách chúng được sử dụng để xử lý và phân tích dữ liệu lớn.</p> <h2 id="introduction-to-big-data">Introduction to big data</h2> <p>Theo <a href="https://www.ibm.com/think/topics/big-data">IBM</a>, dữ liệu lớn (big data) là một tập hợp các dữ liệu có kích thước lớn, tốc độ tăng nhanh và đa dạng, khiến các công cụ phân tích truyền thống không thể xử lý hiệu quả.</p> <p>Dưới đây là 5 chữ V (5 Vs) của dữ liệu lớn, được sử dụng để mô tả các đặc tính của dữ liệu lớn:</p> <h3 id="variety-of-data">Variety of data</h3> <p>Data có thể đến từ nhiều nguồn dữ liệu khác nhau, như là thời tiết, mạng xã hội, cảm biến IoT, giao dịch tài chính, và nhiều nguồn khác. Dữ liệu có thể ở dạng cấu trúc (structured), bán cấu trúc (semi-structured) hoặc phi cấu trúc (unstructured).</p> <h3 id="velocity-of-data">Velocity of data</h3> <p>Data có thể đến từ data warehouse, các file định kỳ hoặc các luồng dữ liệu trực tiếp (streaming data). Dữ liệu có thể được tạo ra với tốc độ cao, ví dụ như dữ liệu từ mạng xã hội hoặc cảm biến IoT. Velocity đề cập đến tốc độ mà dữ liệu được tạo ra và tốc độ mà dữ liệu cần được xử lý và phân tích.</p> <h3 id="volume-of-data">Volume of data</h3> <p>Dữ liệu lớn thường có kích thước rất lớn, từ hàng terabyte đến petabyte hoặc thậm chí exabyte. Volume đề cập đến kích thước của dữ liệu và khả năng lưu trữ và xử lý dữ liệu lớn.</p> <h3 id="veracity-of-data">Veracity of data</h3> <p>Dữ liệu lớn có thể chứa nhiều thông tin không chính xác hoặc không đầy đủ. Veracity đề cập đến độ tin cậy và chất lượng của dữ liệu, và việc xử lý dữ liệu lớn đòi hỏi các kỹ thuật để làm sạch và xác thực dữ liệu.</p> <h3 id="value-of-data">Value of data</h3> <p>Dữ liệu lớn có thể mang lại giá trị lớn nếu được phân tích và sử dụng đúng cách. Value đề cập đến khả năng khai thác giá trị từ dữ liệu lớn thông qua các kỹ thuật phân tích dữ liệu, học máy (machine learning) và trí tuệ nhân tạo (artificial intelligence).</p> <h2 id="the-mapreduce-framework">The MapReduce Framework</h2> <p>MapReduce là một mô hình (framework) được sử dụng để tính toán lượng lớn dữ liệu trong một cụm Hadoop. MapReduces chia quá trình xử lý dữ liệu thành hai bước chính: Map và Reduce. Trong bước Map, dữ liệu được chia thành các cặp key-value và được xử lý song song trên nhiều nút (nodes) trong cụm Hadoop. Trong bước Reduce, các kết quả từ bước Map được tổng hợp lại để tạo ra kết quả cuối cùng.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/hadoop/hadoop-map-reduce-480.webp 480w,/assets/img/posts/hadoop/hadoop-map-reduce-800.webp 800w,/assets/img/posts/hadoop/hadoop-map-reduce-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/hadoop/hadoop-map-reduce.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="MapReduce" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <h2 id="apache-spark">Apache Spark</h2> <p>Apache Spark là một công cụ tính toán phân tán hợp nhất (unified distributed computing engine), được thiết kế để xử lý dữ liệu lớn một cách nhanh chóng và hiệu quả.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/spark/what-is-apache-spark-480.webp 480w,/assets/img/posts/spark/what-is-apache-spark-800.webp 800w,/assets/img/posts/spark/what-is-apache-spark-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/spark/what-is-apache-spark.jpeg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="MapReduce" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p>Apache Spark là một engine xử lý dữ liệu nhanh chóng trên RAM, giúp tăng tốc độ xử lý dữ liệu lớn so với các công cụ truyền thống như Hadoop MapReduce.</p> <p>Spark cung cấp một API dễ sử dụng cho các ngôn ngữ lập trình phổ biến như Java, Scala, Python và R, và hỗ trợ nhiều loại dữ liệu khác nhau, bao gồm dữ liệu cấu trúc, bán cấu trúc và phi cấu trúc. Các thành phần chính của Spark bao gồm Spark Core, Spark SQL, Spark Streaming, MLlib và GraphX. Spark có thể chạy trên nhiều nền tảng khác nhau, bao gồm Hadoop YARN, Apache Mesos và Kubernetes.</p> <h3 id="spark-architecture">Spark Architecture</h3> <p>Một cụm máy tính (cluster) mà Spark sẽ sử dụng để chạy các tasks được quản lý bởi một cluster manager. Chúng ta sẽ submit Spark application đến cluster manager, và cluster manager sẽ phân bổ các resources (CPU, memory) cho Spark application. Spark application sẽ được chia thành nhiều tasks và được chạy trên các worker nodes trong cụm máy tính.</p> <p>Spark Application bao gồm một driver program và nhiều executor processes. Driver program chịu trách nhiệm quản lý các tasks và phân bổ chúng cho các executor processes. Executor processes chịu trách nhiệm thực hiện các tasks và trả về kết quả cho driver program.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/spark/spark-application-480.webp 480w,/assets/img/posts/spark/spark-application-800.webp 800w,/assets/img/posts/spark/spark-application-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/spark/spark-application.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Spark Application" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <h3 id="spark-sql">Spark SQL</h3> <p>Trước Apache Spark, Apache Hive là một công cụ phổ biến để xử lý dữ liệu lớn. Hive cung cấp một ngôn ngữ truy vấn tương tự SQL. Apache Hive dịch các truy vấn SQL thành các công việc MapReduce để xử lý dữ liệu lớn. Tuy nhiên, MapReduce có thể chậm và không hiệu quả cho các truy vấn phức tạp.</p> <p>Với sự ra đời của Apache Spark, Spark SQL đã trở thành một công cụ mạnh mẽ để xử lý dữ liệu lớn. Spark SQL cung cấp một API SQL tương tự như Hive, nhưng với hiệu suất cao hơn nhiều. Spark SQL sử dụng một engine tối ưu hóa (Catalyst optimizer) để tối ưu hóa các truy vấn SQL và thực hiện chúng trên Spark Core.</p> <p>Dưới đây là một sơ đồ minh họa cách Spark SQL hoạt động:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/spark/Catalyst-Optimizer-diagram-480.webp 480w,/assets/img/posts/spark/Catalyst-Optimizer-diagram-800.webp 800w,/assets/img/posts/spark/Catalyst-Optimizer-diagram-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/spark/Catalyst-Optimizer-diagram.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Catalyst optimizer" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <h3 id="spark-dataframe">Spark DataFrame</h3> <p>DataFrame là một cấu trúc dữ liệu trong Spark, tương tự như một bảng trong cơ sở dữ liệu quan hệ (relational database). DataFrame cung cấp một API dễ sử dụng để thao tác và phân tích dữ liệu, bao gồm các phép toán như lọc, nhóm, sắp xếp và tính toán các thống kê. DataFrame có thể được tạo ra từ nhiều nguồn dữ liệu khác nhau, bao gồm các file CSV, JSON, Parquet, Avro và các cơ sở dữ liệu quan hệ. DataFrame cũng hỗ trợ các phép toán SQL, cho phép người dùng viết các truy vấn SQL để thao tác và phân tích dữ liệu trong Spark.</p> <p>Đây là một ví dụ về cách tạo một DataFrame và hiển thị nội dung của nó:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pyspark.sql.types</span> <span class="kn">import</span> <span class="n">StructType</span><span class="p">,</span> <span class="n">StructField</span><span class="p">,</span> <span class="n">StringType</span><span class="p">,</span> <span class="n">IntegerType</span>

<span class="c1"># Define the schema
</span><span class="n">schema</span> <span class="o">=</span> <span class="nc">StructType</span><span class="p">([</span>
    <span class="nc">StructField</span><span class="p">(</span><span class="sh">"</span><span class="s">Name</span><span class="sh">"</span><span class="p">,</span> <span class="nc">StringType</span><span class="p">(),</span> <span class="bp">True</span><span class="p">),</span>
    <span class="nc">StructField</span><span class="p">(</span><span class="sh">"</span><span class="s">Age</span><span class="sh">"</span><span class="p">,</span> <span class="nc">IntegerType</span><span class="p">(),</span> <span class="bp">True</span><span class="p">),</span>
    <span class="nc">StructField</span><span class="p">(</span><span class="sh">"</span><span class="s">City</span><span class="sh">"</span><span class="p">,</span> <span class="nc">StringType</span><span class="p">(),</span> <span class="bp">True</span><span class="p">)</span>
<span class="p">])</span>

<span class="c1"># Create some sample data
</span><span class="n">data</span> <span class="o">=</span> <span class="p">[</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">Alice</span><span class="sh">"</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="sh">"</span><span class="s">New York</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">Bob</span><span class="sh">"</span><span class="p">,</span> <span class="mi">24</span><span class="p">,</span> <span class="sh">"</span><span class="s">London</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">Charlie</span><span class="sh">"</span><span class="p">,</span> <span class="mi">35</span><span class="p">,</span> <span class="sh">"</span><span class="s">New York</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">David</span><span class="sh">"</span><span class="p">,</span> <span class="mi">29</span><span class="p">,</span> <span class="sh">"</span><span class="s">Paris</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">Eve</span><span class="sh">"</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="sh">"</span><span class="s">London</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">Frank</span><span class="sh">"</span><span class="p">,</span> <span class="mi">40</span><span class="p">,</span> <span class="sh">"</span><span class="s">New York</span><span class="sh">"</span><span class="p">)</span>
<span class="p">]</span>

<span class="c1"># Create the DataFrame
</span><span class="n">df</span> <span class="o">=</span> <span class="n">spark</span><span class="p">.</span><span class="nf">createDataFrame</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">schema</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">DataFrame created.</span><span class="sh">"</span><span class="p">)</span>
<span class="n">df</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>
</code></pre></div></div> <table> <thead> <tr> <th>Name</th> <th>Age</th> <th>City</th> </tr> </thead> <tbody> <tr> <td>Alice</td> <td>30</td> <td>New York</td> </tr> <tr> <td>Bob</td> <td>24</td> <td>London</td> </tr> <tr> <td>Charlie</td> <td>35</td> <td>New York</td> </tr> <tr> <td>David</td> <td>29</td> <td>Paris</td> </tr> <tr> <td>Eve</td> <td>30</td> <td>London</td> </tr> <tr> <td>Frank</td> <td>40</td> <td>New York</td> </tr> </tbody> </table> <p>Chúng ta có thể thực hiện các phép toán trên DataFrame, ví dụ tính số lượng người ở mỗi thành phố:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">df_grouped</span> <span class="o">=</span> <span class="n">df</span><span class="p">.</span><span class="nf">groupBy</span><span class="p">(</span><span class="sh">"</span><span class="s">City</span><span class="sh">"</span><span class="p">).</span><span class="nf">count</span><span class="p">()</span>

<span class="c1"># Show the result
</span><span class="n">df_grouped</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>
</code></pre></div></div> <table> <thead> <tr> <th>City</th> <th>count</th> </tr> </thead> <tbody> <tr> <td>London</td> <td>2</td> </tr> <tr> <td>New York</td> <td>3</td> </tr> <tr> <td>Paris</td> <td>1</td> </tr> </tbody> </table> <p>Sử dụng câu lệnh <em>explain</em> để xem kế hoạch thực thi của truy vấn:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">df_grouped</span><span class="p">.</span><span class="nf">explain</span><span class="p">()</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>== Physical Plan ==
AdaptiveSparkPlan isFinalPlan=false
+- HashAggregate(keys=[City#2], functions=[count(1)])
   +- Exchange hashpartitioning(City#2, 200), ENSURE_REQUIREMENTS, [plan_id=71]
      +- HashAggregate(keys=[City#2], functions=[partial_count(1)])
         +- Project [City#2]
            +- Scan ExistingRDD[Name#0,Age#1,City#2]
</code></pre></div></div> <p><a href="https://colab.research.google.com/drive/1Ds8wTnH4l2cexnUO7ZxSdDtLqhL6TeHc?usp=sharing">Source Code</a></p> <h3 id="transformation-và-action">Transformation và Action</h3> <p>Trong Spark, các phép toán trên DataFrame/RDD được chia thành hai loại: <strong>Transformation</strong> (biến đổi) và <strong>Action</strong> (hành động). Việc phân biệt rõ hai loại này là chìa khóa để hiểu cách Spark thực thi công việc.</p> <p><strong>Transformation</strong> là các phép toán tạo ra một DataFrame/RDD mới từ một DataFrame/RDD đã có, ví dụ như <code class="language-plaintext highlighter-rouge">select</code>, <code class="language-plaintext highlighter-rouge">filter</code>, <code class="language-plaintext highlighter-rouge">groupBy</code>, <code class="language-plaintext highlighter-rouge">join</code>, <code class="language-plaintext highlighter-rouge">map</code>. Transformation mang tính chất <strong>lazy evaluation</strong> (đánh giá trễ) — nghĩa là khi gọi một transformation, Spark không thực thi ngay mà chỉ ghi nhận lại phép biến đổi đó vào một kế hoạch thực thi (execution plan), cụ thể là <strong>DAG (Directed Acyclic Graph)</strong>. Đoạn code <code class="language-plaintext highlighter-rouge">df_grouped = df.groupBy("City").count()</code> ở trên chính là một transformation — nó chưa thực sự tính toán gì trên dữ liệu.</p> <p>Transformation lại được chia thành hai loại nhỏ hơn:</p> <ul> <li><strong>Narrow transformation</strong>: mỗi partition đầu vào chỉ đóng góp cho đúng một partition đầu ra (ví dụ <code class="language-plaintext highlighter-rouge">select</code>, <code class="language-plaintext highlighter-rouge">filter</code>, <code class="language-plaintext highlighter-rouge">map</code>). Loại này không cần trộn (shuffle) dữ liệu giữa các executor nên hiệu năng cao.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/spark/narrow-transformation-480.webp 480w,/assets/img/posts/spark/narrow-transformation-800.webp 800w,/assets/img/posts/spark/narrow-transformation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/spark/narrow-transformation.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Narrow Transformation" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <ul> <li><strong>Wide transformation</strong>: dữ liệu từ nhiều partition đầu vào cần được gộp lại để tạo ra partition đầu ra (ví dụ <code class="language-plaintext highlighter-rouge">groupBy</code>, <code class="language-plaintext highlighter-rouge">join</code>, <code class="language-plaintext highlighter-rouge">distinct</code>). Loại này đòi hỏi <strong>shuffle</strong> — di chuyển dữ liệu qua lại giữa các executor trên mạng — nên thường tốn kém hơn về hiệu năng.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/spark/wide-transformation-480.webp 480w,/assets/img/posts/spark/wide-transformation-800.webp 800w,/assets/img/posts/spark/wide-transformation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/spark/wide-transformation.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Wide Transformation" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p><strong>Action</strong> là các phép toán kích hoạt Spark thực sự thực thi toàn bộ DAG đã được xây dựng từ các transformation trước đó, và trả về kết quả cho driver program hoặc ghi dữ liệu ra bên ngoài (file, database…). Một số action phổ biến: <code class="language-plaintext highlighter-rouge">show()</code>, <code class="language-plaintext highlighter-rouge">count()</code>, <code class="language-plaintext highlighter-rouge">collect()</code>, <code class="language-plaintext highlighter-rouge">explain()</code>, <code class="language-plaintext highlighter-rouge">write()</code>. Trong ví dụ trên, <code class="language-plaintext highlighter-rouge">df_grouped.show()</code> chính là action khiến Spark thực sự chạy phép <code class="language-plaintext highlighter-rouge">groupBy</code> và <code class="language-plaintext highlighter-rouge">count</code>.</p> <p>Cơ chế lazy evaluation này cho phép Spark tối ưu hóa toàn bộ chuỗi transformation trước khi thực thi (thông qua Catalyst optimizer đã đề cập ở phần Spark SQL), thay vì thực thi tuần tự từng bước một cách kém hiệu quả.</p>]]></content><author><name>Khanh Xuan Vu</name></author><category term="data-engineering"/><category term="data-engineering"/><category term="data-lake"/><category term="big-data"/><summary type="html"><![CDATA[Tổng quan về phân tích dữ liệu lớn]]></summary></entry><entry><title type="html">Spark SQL</title><link href="https://khanhvx09.github.io/blog/2026/spark-sql/" rel="alternate" type="text/html" title="Spark SQL"/><published>2026-09-22T08:00:00+08:00</published><updated>2026-09-22T08:00:00+08:00</updated><id>https://khanhvx09.github.io/blog/2026/spark-sql</id><content type="html" xml:base="https://khanhvx09.github.io/blog/2026/spark-sql/"><![CDATA[<p>Bài viết này tiếp nối bài <a href="/blog/2026/spark-overview/">Apache Spark Overview</a>, đi sâu vào Spark SQL: cách đọc dữ liệu từ các định dạng phổ biến (CSV, JSON, Parquet) thành bảng, và các mẫu truy vấn SQL thường dùng khi phân tích dữ liệu trên Spark — từ <code class="language-plaintext highlighter-rouge">SELECT</code>, <code class="language-plaintext highlighter-rouge">JOIN</code>, <code class="language-plaintext highlighter-rouge">GROUP BY</code> cơ bản đến window function và subquery.</p> <h2 id="create-table">Create table</h2> <p>Giới thiệu 3 định dạng dữ liệu phổ biến và cách đọc mỗi loại thành DataFrame bằng <code class="language-plaintext highlighter-rouge">spark.read</code>, sau đó đăng ký thành bảng/view để truy vấn bằng SQL qua <code class="language-plaintext highlighter-rouge">spark.sql(...)</code>.</p> <ul> <li>Đọc CSV (<code class="language-plaintext highlighter-rouge">header</code>, <code class="language-plaintext highlighter-rouge">inferSchema</code>, <code class="language-plaintext highlighter-rouge">sep</code>)</li> <li>Đọc Parquet (schema tự mô tả, columnar, nén tốt — so sánh nhanh với CSV/JSON)</li> </ul> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">IF</span> <span class="k">NOT</span> <span class="k">EXISTS</span> <span class="n">employees</span> <span class="p">(</span>
  <span class="n">id</span>      <span class="nb">INT</span>     <span class="k">COMMENT</span> <span class="s1">'Mã nhân viên, khoá chính'</span><span class="p">,</span>
  <span class="n">name</span>    <span class="n">STRING</span>  <span class="k">COMMENT</span> <span class="s1">'Họ tên đầy đủ'</span><span class="p">,</span>
  <span class="n">city</span>    <span class="n">STRING</span>  <span class="k">COMMENT</span> <span class="s1">'Thành phố nơi làm việc'</span><span class="p">,</span>
  <span class="n">salary</span>  <span class="nb">DOUBLE</span>  <span class="k">COMMENT</span> <span class="s1">'Lương theo tháng, đơn vị USD'</span>
<span class="p">)</span>
<span class="k">COMMENT</span> <span class="s1">'Bảng thông tin nhân viên, cập nhật hàng ngày từ hệ thống HR'</span>
<span class="n">STORED</span> <span class="k">AS</span> <span class="n">PARQUET</span>
<span class="k">LOCATION</span> <span class="s1">'path/to/employees'</span><span class="p">;</span>

<span class="c1">-- Xem lại comment của bảng và từng cột</span>
<span class="k">DESCRIBE</span> <span class="k">TABLE</span> <span class="n">EXTENDED</span> <span class="n">employees</span><span class="p">;</span>
</code></pre></div></div> <p>Với file CSV dùng ký tự phân tách khác dấu phẩy (ví dụ <code class="language-plaintext highlighter-rouge">|</code>), khai báo thêm <code class="language-plaintext highlighter-rouge">delimiter</code> (hoặc <code class="language-plaintext highlighter-rouge">sep</code>) trong <code class="language-plaintext highlighter-rouge">OPTIONS</code>:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">IF</span> <span class="k">NOT</span> <span class="k">EXISTS</span> <span class="n">orders</span> <span class="p">(</span>
  <span class="n">order_id</span>     <span class="nb">INT</span>     <span class="k">COMMENT</span> <span class="s1">'Mã đơn hàng, khoá chính'</span><span class="p">,</span>
  <span class="n">customer_id</span>  <span class="nb">INT</span>     <span class="k">COMMENT</span> <span class="s1">'Mã khách hàng'</span><span class="p">,</span>
  <span class="n">product</span>      <span class="n">STRING</span>  <span class="k">COMMENT</span> <span class="s1">'Tên sản phẩm'</span><span class="p">,</span>
  <span class="n">amount</span>       <span class="nb">DOUBLE</span>  <span class="k">COMMENT</span> <span class="s1">'Giá trị đơn hàng, đơn vị USD'</span>
<span class="p">)</span>
<span class="k">USING</span> <span class="n">csv</span>
<span class="k">COMMENT</span> <span class="s1">'Bảng đơn hàng, dữ liệu nguồn dạng CSV phân tách bằng dấu |'</span>
<span class="k">OPTIONS</span> <span class="p">(</span>
  <span class="n">path</span> <span class="nv">"path/to/orders"</span><span class="p">,</span>
  <span class="n">header</span> <span class="nv">"true"</span><span class="p">,</span>
  <span class="k">delimiter</span> <span class="nv">"|"</span>
<span class="p">);</span>

<span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="n">orders</span> <span class="k">LIMIT</span> <span class="mi">10</span><span class="p">;</span>
</code></pre></div></div> <blockquote> <p><strong>Lưu ý:</strong> khi đặt tên bảng, nên theo format <code class="language-plaintext highlighter-rouge">[source_system]_[table_name]</code> (ví dụ <code class="language-plaintext highlighter-rouge">hr_employees</code>, <code class="language-plaintext highlighter-rouge">erp_orders</code>) thay vì chỉ dùng tên bảng đơn thuần như <code class="language-plaintext highlighter-rouge">employees</code>, <code class="language-plaintext highlighter-rouge">orders</code>. Cách đặt tên này giúp:</p> <ul> <li>Biết ngay dữ liệu bảng đến từ hệ thống nguồn nào chỉ bằng cách nhìn tên bảng, không cần tra lineage/metadata.</li> <li>Tránh đụng tên khi nhiều hệ thống nguồn cùng có bảng trùng tên (ví dụ cả HR và ERP đều có bảng <code class="language-plaintext highlighter-rouge">employees</code>).</li> <li>Dễ nhóm, filter bảng theo nguồn khi liệt kê danh sách bảng trong catalog.</li> </ul> </blockquote> <h2 id="select-và-các-phép-lọc-cơ-bản">SELECT và các phép lọc cơ bản</h2> <p>Cú pháp <code class="language-plaintext highlighter-rouge">SELECT ... FROM ... WHERE ...</code> trong Spark SQL, chọn cột, đặt alias, lọc điều kiện, <code class="language-plaintext highlighter-rouge">ORDER BY</code>, <code class="language-plaintext highlighter-rouge">LIMIT</code>, <code class="language-plaintext highlighter-rouge">DISTINCT</code>.</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="n">column1</span><span class="p">,</span> <span class="n">column2</span> <span class="k">AS</span> <span class="n">alias_name</span>
<span class="k">FROM</span> <span class="n">my_table</span>
<span class="k">WHERE</span> <span class="n">condition</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">column1</span> <span class="k">DESC</span>
<span class="k">LIMIT</span> <span class="mi">10</span><span class="p">;</span>
</code></pre></div></div> <h2 id="join">JOIN</h2> <p>Các loại join: <code class="language-plaintext highlighter-rouge">INNER JOIN</code>, <code class="language-plaintext highlighter-rouge">LEFT/RIGHT OUTER JOIN</code>, <code class="language-plaintext highlighter-rouge">FULL OUTER JOIN</code>, <code class="language-plaintext highlighter-rouge">CROSS JOIN</code>, và lưu ý về shuffle khi join hai bảng lớn (liên hệ lại phần wide transformation ở bài overview).</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="n">a</span><span class="p">.</span><span class="o">*</span><span class="p">,</span> <span class="n">b</span><span class="p">.</span><span class="o">*</span>
<span class="k">FROM</span> <span class="n">table_a</span> <span class="n">a</span>
<span class="k">INNER</span> <span class="k">JOIN</span> <span class="n">table_b</span> <span class="n">b</span>
  <span class="k">ON</span> <span class="n">a</span><span class="p">.</span><span class="n">id</span> <span class="o">=</span> <span class="n">b</span><span class="p">.</span><span class="n">id</span><span class="p">;</span>
</code></pre></div></div> <h2 id="group-by-và-having">GROUP BY và HAVING</h2> <p>Tổng hợp dữ liệu theo nhóm với <code class="language-plaintext highlighter-rouge">GROUP BY</code> và các hàm aggregate (<code class="language-plaintext highlighter-rouge">COUNT</code>, <code class="language-plaintext highlighter-rouge">SUM</code>, <code class="language-plaintext highlighter-rouge">AVG</code>, <code class="language-plaintext highlighter-rouge">MAX</code>, <code class="language-plaintext highlighter-rouge">MIN</code>), lọc kết quả sau khi group bằng <code class="language-plaintext highlighter-rouge">HAVING</code> (khác với <code class="language-plaintext highlighter-rouge">WHERE</code> lọc trước khi group).</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="n">city</span><span class="p">,</span> <span class="k">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="k">AS</span> <span class="n">num_people</span><span class="p">,</span> <span class="k">AVG</span><span class="p">(</span><span class="n">age</span><span class="p">)</span> <span class="k">AS</span> <span class="n">avg_age</span>
<span class="k">FROM</span> <span class="n">my_table</span>
<span class="k">GROUP</span> <span class="k">BY</span> <span class="n">city</span>
<span class="k">HAVING</span> <span class="k">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div> <h2 id="window-function">Window Function</h2> <p>Khái niệm window function: tính toán trên một “cửa sổ” các dòng liên quan mà không gộp (collapse) dòng như <code class="language-plaintext highlighter-rouge">GROUP BY</code>. Cú pháp <code class="language-plaintext highlighter-rouge">OVER (PARTITION BY ... ORDER BY ...)</code>.</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span>
  <span class="n">name</span><span class="p">,</span>
  <span class="n">city</span><span class="p">,</span>
  <span class="n">salary</span><span class="p">,</span>
  <span class="k">AVG</span><span class="p">(</span><span class="n">salary</span><span class="p">)</span> <span class="n">OVER</span> <span class="p">(</span><span class="k">PARTITION</span> <span class="k">BY</span> <span class="n">city</span><span class="p">)</span> <span class="k">AS</span> <span class="n">avg_salary_by_city</span>
<span class="k">FROM</span> <span class="n">my_table</span><span class="p">;</span>
</code></pre></div></div> <h2 id="rank-dense_rank-row_number">RANK, DENSE_RANK, ROW_NUMBER</h2> <p>So sánh 3 hàm xếp hạng thường bị nhầm lẫn:</p> <ul> <li><code class="language-plaintext highlighter-rouge">ROW_NUMBER()</code>: đánh số thứ tự duy nhất, không có giá trị trùng nhau</li> <li><code class="language-plaintext highlighter-rouge">RANK()</code>: các dòng bằng nhau nhận cùng hạng, hạng tiếp theo bị nhảy số</li> <li><code class="language-plaintext highlighter-rouge">DENSE_RANK()</code>: các dòng bằng nhau nhận cùng hạng, hạng tiếp theo không bị nhảy số</li> </ul> <p>Có thể minh họa bằng một bảng ví dụ trước/sau khi áp dụng cả 3 hàm trên cùng một tập dữ liệu có giá trị trùng nhau.</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span>
  <span class="n">name</span><span class="p">,</span>
  <span class="n">city</span><span class="p">,</span>
  <span class="n">salary</span><span class="p">,</span>
  <span class="n">ROW_NUMBER</span><span class="p">()</span> <span class="n">OVER</span> <span class="p">(</span><span class="k">PARTITION</span> <span class="k">BY</span> <span class="n">city</span> <span class="k">ORDER</span> <span class="k">BY</span> <span class="n">salary</span> <span class="k">DESC</span><span class="p">)</span> <span class="k">AS</span> <span class="n">row_num</span><span class="p">,</span>
  <span class="n">RANK</span><span class="p">()</span>       <span class="n">OVER</span> <span class="p">(</span><span class="k">PARTITION</span> <span class="k">BY</span> <span class="n">city</span> <span class="k">ORDER</span> <span class="k">BY</span> <span class="n">salary</span> <span class="k">DESC</span><span class="p">)</span> <span class="k">AS</span> <span class="n">rnk</span><span class="p">,</span>
  <span class="n">DENSE_RANK</span><span class="p">()</span> <span class="n">OVER</span> <span class="p">(</span><span class="k">PARTITION</span> <span class="k">BY</span> <span class="n">city</span> <span class="k">ORDER</span> <span class="k">BY</span> <span class="n">salary</span> <span class="k">DESC</span><span class="p">)</span> <span class="k">AS</span> <span class="n">dense_rnk</span>
<span class="k">FROM</span> <span class="n">my_table</span><span class="p">;</span>
</code></pre></div></div> <h2 id="subquery">Subquery</h2> <p>Subquery trong <code class="language-plaintext highlighter-rouge">WHERE</code> (scalar/<code class="language-plaintext highlighter-rouge">IN</code>), subquery trong <code class="language-plaintext highlighter-rouge">FROM</code> (derived table), và correlated subquery (subquery tham chiếu đến cột của query bên ngoài).</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="n">name</span>
<span class="k">FROM</span> <span class="n">my_table</span>
<span class="k">WHERE</span> <span class="n">salary</span> <span class="o">&gt;</span> <span class="p">(</span><span class="k">SELECT</span> <span class="k">AVG</span><span class="p">(</span><span class="n">salary</span><span class="p">)</span> <span class="k">FROM</span> <span class="n">my_table</span><span class="p">);</span>
</code></pre></div></div> <h2 id="with-common-table-expression">WITH (Common Table Expression)</h2> <p>Dùng <code class="language-plaintext highlighter-rouge">WITH</code> để đặt tên cho một subquery, giúp truy vấn dễ đọc hơn khi cần tái sử dụng cùng một tập kết quả trung gian nhiều lần.</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">WITH</span> <span class="n">city_avg</span> <span class="k">AS</span> <span class="p">(</span>
  <span class="k">SELECT</span> <span class="n">city</span><span class="p">,</span> <span class="k">AVG</span><span class="p">(</span><span class="n">salary</span><span class="p">)</span> <span class="k">AS</span> <span class="n">avg_salary</span>
  <span class="k">FROM</span> <span class="n">my_table</span>
  <span class="k">GROUP</span> <span class="k">BY</span> <span class="n">city</span>
<span class="p">)</span>
<span class="k">SELECT</span> <span class="n">t</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">t</span><span class="p">.</span><span class="n">city</span><span class="p">,</span> <span class="n">t</span><span class="p">.</span><span class="n">salary</span><span class="p">,</span> <span class="k">c</span><span class="p">.</span><span class="n">avg_salary</span>
<span class="k">FROM</span> <span class="n">my_table</span> <span class="n">t</span>
<span class="k">JOIN</span> <span class="n">city_avg</span> <span class="k">c</span> <span class="k">ON</span> <span class="n">t</span><span class="p">.</span><span class="n">city</span> <span class="o">=</span> <span class="k">c</span><span class="p">.</span><span class="n">city</span>
<span class="k">WHERE</span> <span class="n">t</span><span class="p">.</span><span class="n">salary</span> <span class="o">&gt;</span> <span class="k">c</span><span class="p">.</span><span class="n">avg_salary</span><span class="p">;</span>
</code></pre></div></div> <h2 id="view-và-temporary-view">View và Temporary View</h2> <p>Phân biệt <code class="language-plaintext highlighter-rouge">createOrReplaceTempView</code> (chỉ tồn tại trong session hiện tại) và <code class="language-plaintext highlighter-rouge">createOrReplaceGlobalTempView</code> (dùng chung giữa nhiều session, lưu trong database <code class="language-plaintext highlighter-rouge">global_temp</code>). So sánh với view thông thường trong các hệ quản trị cơ sở dữ liệu (database view).</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">df</span><span class="p">.</span><span class="nf">createOrReplaceTempView</span><span class="p">(</span><span class="sh">"</span><span class="s">my_table</span><span class="sh">"</span><span class="p">)</span>
<span class="n">df</span><span class="p">.</span><span class="nf">createOrReplaceGlobalTempView</span><span class="p">(</span><span class="sh">"</span><span class="s">my_global_table</span><span class="sh">"</span><span class="p">)</span>

<span class="n">spark</span><span class="p">.</span><span class="nf">sql</span><span class="p">(</span><span class="sh">"</span><span class="s">SELECT * FROM my_table</span><span class="sh">"</span><span class="p">)</span>
<span class="n">spark</span><span class="p">.</span><span class="nf">sql</span><span class="p">(</span><span class="sh">"</span><span class="s">SELECT * FROM global_temp.my_global_table</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>]]></content><author><name>Khanh Xuan Vu</name></author><category term="data-engineering"/><category term="data-engineering"/><category term="data-lake"/><category term="big-data"/><category term="spark"/><category term="sql"/><summary type="html"><![CDATA[Thao tác với dữ liệu có cấu trúc bằng Spark SQL]]></summary></entry></feed>